package coolpharaoh.tee.speicher.tea.timer.viewmodels;

//@RunWith(AndroidJUnit4.class)
public class CountDownServiceViewModelTest {
/*
    CountDownServiceViewModel mCountDownServiceViewModel;

    @Before
    public void createDb() {
        Context context = ApplicationProvider.getApplicationContext();

        mCountDownServiceViewModel = new CountDownServiceViewModel(context);
    }

    @After
    public void closeDb() {
    }

    @Test
    public void insertCounter(){
        ActualSettings actualSettings = new ActualSettings();
        actualSettings.setNotification(true);

        ActualSettingsDAO actualSettingsDAO = Mockito.mock(ActualSettingsDAO.class);
        Mockito.when(actualSettingsDAO.getSettings()).thenReturn(actualSettings);

        assertEquals(mCountDownServiceViewModel.isNotification(), true);

        actualSettings.setNotification(false);

        assertEquals(mCountDownServiceViewModel.isNotification(), false);
    }
*/
}
