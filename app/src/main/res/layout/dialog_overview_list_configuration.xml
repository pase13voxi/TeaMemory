<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fadeScrollbars="false"
    android:scrollbarSize="4dp"
    android:scrollbarThumbVertical="@color/background_grey">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            style="@style/overview_linear_layout_heading"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/sort_black" />

            <TextView
                style="@style/overview_text_view_heading"
                android:id="@+id/text_view_overview_sort_mode_configuration_title"
                android:text="@string/overview_dialog_sort_title" />
        </LinearLayout>

        <RadioGroup
            android:id="@+id/radio_group_overview_sort_mode"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="vertical" />

        <LinearLayout
            style="@style/overview_linear_layout_heading"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/sort_black" />

            <TextView
                style="@style/overview_text_view_heading"
                android:id="@+id/text_view_overview_filter_configuration_title"
                android:text="@string/overview_dialog_filter_title" />
        </LinearLayout>

        <CheckBox
            android:id="@+id/checkbox_overview_in_stock"
            style="@style/overview_checkbox_in_stock"
            android:text="@string/overview_dialog_show_teas_in_stock"
            android:theme="@style/check_box_style" />

    </LinearLayout>
</ScrollView>